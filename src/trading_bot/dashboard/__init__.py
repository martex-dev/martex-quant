"""Local operations dashboard: one page to observe and drive the system.

Read-mostly by design; the only actions it exposes are the same safe
commands available on the CLI (paper run, dry-run, guard check, tests).
Going live is deliberately NOT a button.
"""
